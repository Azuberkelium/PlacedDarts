body {
    font-family: Arial, sans-serif;
    display: flex;
    flex-direction: column;
    align-items: center;
    background-color: #f0f0f0;
    margin: 0;
    padding: 20px;
}

h1 {
    color: #333;
}

#dartboard-container {
    position: relative;
    width: 450px;
    height: 450px;
    border-radius: 50%;
    background-color: #333;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 20px;
}

.bullseye-25 {
    position: absolute;
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background-color: #0c0;
    cursor: pointer;
    z-index: 3;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    font-weight: bold;
    font-size: 1.2em;
}

.bullseye {
    position: absolute;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: #f00;
    cursor: pointer;
    z-index: 4;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    font-weight: bold;
    font-size: 1.2em;
}

.dart-segment {
    position: absolute;
    width: 450px;
    height: 450px;
    clip-path: polygon(50% 50%, 50% 0, 100% 0, 100% 100%, 0 100%, 0 0); /* This is a placeholder, JS will set the real clip-path */
    transform-origin: 50% 50%;
    cursor: pointer;
    overflow: hidden;
    color: #fff;
    font-weight: bold;
    text-align: center;
}

.single-section {
    position: absolute;
    height: 100%;
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    pointer-events: none;
}

.outer-single-text {
    position: absolute;
    font-size: 1em;
    bottom: 20px;
    transform: rotate(90deg);
}

.inner-single-text {
    position: absolute;
    font-size: 1em;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) rotate(90deg);
}

.triple-ring, .double-ring {
    position: absolute;
    width: 100%;
    height: 100%;
    pointer-events: none;
}

/* Base colors for segments */
.black-segment { background-color: #333; }
.white-segment { background-color: #fff; }
.red-segment { background-color: #f00; }
.green-segment { background-color: #0c0; }

.score-display {
    margin-top: 30px;
}

.coin-container {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    background-color: #ffcc00;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 2em;
    font-weight: bold;
    color: #333;
    box-shadow: 0 0 5px rgba(0, 0, 0, 0.3);
}

.player-score-container {
    margin-top: 20px;
    font-size: 1.5em;
    display: flex;
    align-items: center;
}

.controls {
    margin-top: 20px;
}

.controls button {
    padding: 10px 20px;
    font-size: 1em;
    cursor: pointer;
    margin: 0 10px;
    border: 1px solid #ccc;
    background-color: #eee;
    border-radius: 5px;
}
